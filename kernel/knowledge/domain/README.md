# Knowledge Domain

Reserved for derived graph/read-model concepts. It must not become a source of truth.

```mermaid
flowchart LR
  Assertion[Custody Assertion] --> DerivedRelation[Derived Relation]
  DerivedRelation --> Query[Query]
```

